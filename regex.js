let s1 = 'Authentication for powering off of system';
let s2 = 'Authentication for restaring of system';
let s3 = "Athentication for set in-wall of system";

print(s1.search(/(powering\soff)|(restaring)/));
print(s2.search(/(powering\soff)|(restaring)/));
print(s3.search(/(powering\soff)|(restaring)/));
